# GitHub repository initialization checkpoint

This repository root is the canonical starter snapshot prepared on 2026-08-25 after P1-080.

Expected clean remote before upload:
- repository: `lukindv77/webclip-pdf`
- branch: `main`
- HEAD: `e4ae66c6b677c6e49841caf3c9aa5ce7e970fa7a`

The prior incomplete `.bootstrap` transport and temporary sync workflow were removed from active `main` by force-resetting to that clean commit.
They must not be restored.

Manifest gate remains MV3 / version `0.9.8` until release QA.
