# C16 / P1-150 reactivation evidence receipt — 2026-09-02

Status: **durable exact-evidence receipt**. `RESEARCH_REGISTRY.md` remains the sole current P-owner/status authority.

This receipt accompanies `RESEARCH_FULL_RESTART_C16_SAME_ORIGIN_IFRAME_2026-09-02.md` and exists to bind the P1-150 Registry reactivation to exact current-source and physical evidence without duplicating the full C16 matrix.

## Exact current-source baseline

- canonical tranche baseline: `main = 2bdd469be3e6a5a14f70123d42ec7fa01a7994e8`;
- exact `content.js` Git blob: `f3ee7b51fe9ee94fdfe36e8a7c99f14a548fdc4e`;
- current source contains the selected same-origin live-frame stabilization mechanism and related `Math.min(200000, ...)` height clamps.

## Accepted physical evidence

- exact evidence head: `bda5f1e655e7276d6654a0a0051f97a184c4bb85`;
- workflow run: `33593581470`;
- job: `100132387642`;
- browser: Google Chrome `151.0.7922.173`;
- conclusion: **SUCCESS**;
- raw result JSON SHA-256: `73cc3a7f59c42cfa906cbc57132ee77c286199211df3c30ee544427975e05de3`.

The ordinary long one-level BODY, long non-BODY, nested BODY and nested non-BODY same-origin controls preserve FIRST/MIDDLE/LAST through physical PDF, rejecting the broad hypothesis that current live/nested same-origin iframe pagination is universally incomplete.

The discriminating over-bound current-path case requires about `210016px`, current stabilization applies about `200004px`, and physical PDF loses the LAST sentinel/tail (`3984/4200` extracted rows; last extracted row `4000`). The causal top-document normal-flow materialization of the same selected content preserves `4200/4200` plus FIRST/MIDDLE/LAST while keeping Exclude and the unselected top shell out.

## Owner reconciliation

This is not a new P-code. Git/current-history reconciliation identifies historical **P1-150** as the specific owner for selected same-origin iframe print-height stabilization. Fresh current-source L4 evidence demonstrates that its acceptance contract is unresolved, so the canonical Registry reactivation is:

> **P1-150 ACTIVE** — Same-origin selected iframe print-height stabilization must not silently truncate admitted selected content at the `200000px` guard; over-bound content requires a complete bounded final representation or a truthful degraded/failed outcome instead of partial-PDF success.

**P0-004 ACTIVE** remains supporting ownership for physical selected-PDF completeness. P1-187/P1-226/P1-227 remain adjacent owners; P0-064 and P0-068 remain closed. No new owner is allocated.

## Architecture boundary

The evidence does not justify an unbounded iframe height or universal flattening. A valid implementation may retain hard budgets, but exceeding the live-frame representation envelope must settle through a bounded complete final representation or a truthful degraded/failed result, never silent partial success.

No runtime source, manifest/version, build/tag/GitHub Release, or release-readiness state is changed by this evidence receipt.
