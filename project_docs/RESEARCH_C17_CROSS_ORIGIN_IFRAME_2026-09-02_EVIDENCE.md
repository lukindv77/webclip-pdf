# C17 cross-origin iframe — durable owner evidence receipt — 2026-09-02

Purpose: machine-recognized durable owner-impact receipt for the fresh-restart C17 tranche. Detailed source proof, physical matrix, rejected-development-run provenance, duplicate reconciliation and architecture implications remain in `RESEARCH_FULL_RESTART_C17_CROSS_ORIGIN_IFRAME_2026-09-02.md`.

Canonical source researched: `f1f5de60d7de901eb8249ae3376b07c29a196961`.

Exact product blobs:

- `content.js`: `f3ee7b51fe9ee94fdfe36e8a7c99f14a548fdc4e`;
- `frame-agent.js`: `ce55145dc7ee1a4abf485b7fad3134ac39b61751`.

Accepted physical boundary:

- Google Chrome `151.0.7922.173`;
- workflow run `33596133152`;
- job `100139834463`;
- exact run head `ceb56406d03d0503c6e898e1fa940bd5ef70d3f7`;
- conclusion `SUCCESS`;
- result JSON SHA-256 `e85ec2f6f72e204de597ba5811abe27e7d1dd9db3bca448c7f83ab72948fe896`.

Owner impact:

- **P1-229 ACTIVE** — freshly revalidated. The worker-shaped `screen` media cut physically preserved all 120 selected rows but also printed the explicit Exclude and 159 unselected child rows. The same already-prepared document under causal `print` media preserved 120/120 selected rows while excluding both classes of unwanted content. Current source independently shows remote height is measured before the selected-only `@media print` representation is installed. No Registry wording/status transition is needed because P1-229 already exactly owns this media/geometry contract.
- **P1-004 ACTIVE** — umbrella strengthened. In topology `top A -> cross-origin outer B -> same-origin inner B`, the outer frame-agent remained active/selectable while the inner agent self-exited due immediate-parent same-origin access and top recursion remained blocked by the outer SOP boundary. Inner selection stayed unchanged; the outer positive control succeeded. No more precise existing owner supersedes this feature-topology observation, and no new P-code is allocated.

Adjacent lifecycle owners P1-171, P1-193, P1-199, P1-200, P1-201, P1-203 and P1-214 remain independently ACTIVE and are not claimed closed or fully exercised by C17.

Integrated campaign classification:

`L4-REVALIDATED / FINDING + POSITIVE/CAUSAL/NESTED-BOUNDARY CONTROLS (P1-229, P1-004 umbrella)`

Runtime, manifest version `0.9.8`, release readiness, build, tag and GitHub Release are unchanged.
