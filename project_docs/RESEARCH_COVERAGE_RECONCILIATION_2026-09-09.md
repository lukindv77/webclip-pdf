# WebClip — current project-wide research coverage reconciliation — 2026-09-09

Date: 2026-09-09  
Canonical GitHub source at reconciliation start: `main = d4f5b268fa3f7ced5a7bc68da52784863d614138`  
Research branch: `research/pd7-single-axis-scroll-stable-2026-09-09`  
Registry authority: `project_docs/RESEARCH_REGISTRY.md`  
Fresh external input: `project_docs/RESEARCH_EXTERNAL_USER_INTENT_BASELINE_2026-09-09.md`  
PD7 Change Impact: `project_docs/RESEARCH_PD7_SINGLE_AXIS_SCROLL_CURRENT_CHANNEL_2026-09-09.md`

This reconciliation is research/process state only. It does not close ACTIVE P-owners, edit runtime, change release readiness or authorize a build/release.

## 1. Why a new reconciliation is required

The Cycle-2 final synthesis dated 2026-09-01 correctly declared `DEEP-RESEARCH-COVERAGE-COMPLETE` for its exact campaign baseline and Change Impact.

That statement was explicitly bounded: later runtime, contract, browser/platform or external changes can reopen affected cells.

Since that checkpoint:

1. canonical `main` advanced by many commits, including production/runtime changes in Journal/offscreen/guard/integration surfaces;
2. a fresh full-restart campaign independently revalidated most C01…C46 families against newer current source;
3. the external baseline from 2026-08-31 became older than the project's active-research freshness target and has now been refreshed on 2026-09-09;
4. PD7 Single-axis scroll containers moved from a simple old WATCH assumption to an operationally rollout-sensitive current-channel Change Impact requiring exact browser feature detection.

Therefore the old Cycle-2 “platform delta = 0 / runtime staleness = none” conclusion cannot be copied onto current `main`.

## 2. Current fresh-restart denominator

The current full-restart baseline retains the complete C01…C46 family denominator.

The fresh campaign has already independently revalidated essentially all in-repo/managed-browser families at their required source/L3/L4 level. The important residual states are not hidden:

- C41 — partial, native/restart L5 remains;
- C42 — canonical restart row still says `NOT-TRIAGED / UNKNOWN`;
- C43 — partial, real Yandex object/publication linkage remains external;
- C44 — partial, remaining native/remote recovery boundaries remain;
- C45 — partial, native/later-reader interaction boundaries remain where specified;
- C46 — partial, real permission/debugger/restart/interactive browser boundaries remain.

Separately, PD7 now reopens a targeted browser-platform variant under C20/C31 and supporting C22/C23/C29.

## 3. Current terminality interpretation

A research family does **not** need a product fix to be research-terminal. A proven FINDING with canonical owner can be terminal for coverage while implementation remains ACTIVE.

Likewise an external/native boundary can be research-terminal only when the repository states explicitly:

```text
EXTERNAL-REQUIRED / UNKNOWN
```

or an equivalent bounded state with:

- exact reason local/managed evidence is insufficient;
- exact external environment needed;
- exact receipt required;
- no synthetic lower-level PASS substituted for it.

The current reconciliation therefore separates:

```text
research coverage terminality
from
finding implementation closure
from
release readiness
```

## 4. PD7 is a real current Change Impact blocker

Associated branch evidence proves:

- semantic model PASS on exact committed bytes;
- Chromium-144 physical negative platform control on exact committed harness bytes;
- current source lacks explicit per-axis scroll-container / axis-qualified user-reach architecture;
- exact current-channel rollout is ambiguous across official Chrome surfaces;
- a feature-active/current-target browser run has not yet been obtained.

Therefore PD7 is currently:

```text
REVALIDATION-REQUIRED / ROLLOUT-SENSITIVE
```

Affected coverage interpretation:

```text
C20 = existing L4 FINDING + PD7 physical revalidation pending
C31 = existing L4 FINDING + PD7 physical revalidation pending
C22/C23 = P1-230 acceptance refined by per-axis context; no new owner
C29 = supporting geometry Change Impact
```

This alone prevents an unconditional new project-wide `DEEP-RESEARCH-COVERAGE-COMPLETE` declaration today.

## 5. C42 is the other explicit coverage gate

The full-restart matrix still records:

```text
C42 | Yandex upload/object/public identity | NOT-TRIAGED / UNKNOWN
```

Current Registry already has relevant owners including:

- P0-073 — immutable account/root scope for unresolved remote-save recovery;
- P0-074 — immutable Yandex auth/account/root/config/publication operation context;
- P0-078 — public-link generation/revocation policy;
- P1-184 — stronger exact remote object/content creation receipt.

Official Yandex Disk API documentation confirms that the service exposes REST access for user files/uploads and publication semantics, but public documentation cannot prove WebClip's real object/account/public-link settlement for one actual operation.

Therefore the remaining C42 evidence is inherently external/account-bound.

### Correct C42 research direction

There are only two truthful paths:

#### Path A — execute real L5 evidence

With an explicitly authorized non-production/test Yandex context:

```text
exact PDF digest
+ immutable account/root/config generation
+ exact upload operation
+ exact remote object identity
+ exact publication/public URL outcome
+ retry/unknown reconciliation
```

must be recorded.

#### Path B — explicitly terminalize as bounded external UNKNOWN

If real authorized account evidence is intentionally deferred, canonical coverage state must say, in substance:

```text
EXTERNAL-REQUIRED / UNKNOWN
```

with the exact L5 receipt above.

A bare `NOT-TRIAGED / UNKNOWN` remains nonterminal under project coverage policy.

This research branch does **not** rewrite the canonical matrix. It records the reconciliation requirement.

## 6. C41/C43/C44/C45/C46 are not hidden research gaps

Their current fresh-restart rows already contain concrete managed/in-repo findings and explicit residual native/remote boundaries.

They remain release/closure risks, but they do not justify starting arbitrary additional local renderer tranches merely because implementation is not complete.

Any final coverage synthesis must preserve their exact external/partial status rather than upgrade them to full PASS.

## 7. External baseline freshness is restored

`RESEARCH_EXTERNAL_USER_INTENT_BASELINE_2026-09-09.md` is now the current external risk-ranking input.

Fresh observations:

- Chrome current-channel single-axis rollout ambiguity / PD7 Change Impact;
- Chrome 154 Beta WATCH variants;
- current SingleFile movement and documented event-timed frame incompleteness;
- Obsidian first-vs-repeat capture divergence and credential-storage trust signal;
- Browsertrix capture-vs-replay differential QA pattern.

No fresh external observation proves a new independent WebClip root cause or user-intent requirement. No new P-code is allocated.

## 8. Current project-wide research state

The strongest truthful state at this checkpoint is:

```text
DEEP-RESEARCH-IN-PROGRESS / RECONCILIATION-BLOCKED
```

with exactly identified blockers:

1. **PD7 feature-active/current-target physical revalidation** or an explicit checkpoint proving the feature is not active in the selected Stable target;
2. **C42 terminal classification/evidence** — real authorized L5 evidence or explicit canonical `EXTERNAL-REQUIRED / UNKNOWN` boundary.

This is intentionally stronger and more precise than either:

- falsely carrying forward the old coverage-complete declaration; or
- pretending that the entire project must be researched from zero again.

## 9. What is already saturated

Fresh-restart evidence has already revalidated the broad in-repo/managed-browser families. The long P1-216…P1-230 source-first sequence also converted multiple durable observations into explicit current-source models/gates without production edits.

The next research work should therefore be **blocker-driven**, not enumeration-driven.

No `P1-231` should be created unless a new observation survives duplicate reconciliation and cannot be represented by existing owners.

## 10. Exact next evidence for PD7

A current-target browser receipt must record:

```text
browser executable/version/channel
CSS.supports("named-feature(single-axis-scroll-container)")
computed overflow x/y
programmatic clipped-axis settlement
per-axis sticky geometry
raw selected physical PDF
static-normalized control PDF
```

If feature detection is false on the current Stable target, the checkpoint must record that fact rather than infer support from major version/release notes.

If true, execute the full PD7 A–H schedules defined in the PD7 document.

## 11. Exact next evidence for C42

If an authorized Yandex test context becomes available, the L5 operation should bind:

```text
operationId
PDF digest/size
account identity receipt (non-secret)
root/config generation
remote path/object identity
server object metadata sufficient for exact reconciliation
publication requested? yes/no
public URL/publication outcome
retry/unknown-recovery outcome
Journal/OperationLog exact operation linkage
```

Secrets/tokens must not be committed into evidence.

If this test is deferred, canonical coverage should explicitly preserve C42 as bounded external UNKNOWN.

## 12. Implementation stream remains separate

Many P0/P1 owners are ACTIVE. This reconciliation does not imply they should wait for coverage blockers before implementation.

Engineering can continue risk-ranked implementation/Closure Sweep in parallel, provided every implementation invokes Change Impact for the coverage regions it modifies.

Research completion and implementation closure are orthogonal gates.

## 13. Release state

No change:

```text
DEEP-RESEARCH-CRITICAL-CLOSURE-COMPLETE = NO
RELEASE-READY = NO
```

No build, tag, GitHub Release or deployment is justified by this research branch.

## 14. Reconciliation decision

At 2026-09-09 on canonical `main = d4f5b268fa3f7ced5a7bc68da52784863d614138`:

- external research freshness: **restored**;
- broad fresh-restart in-repo coverage: **substantially saturated**;
- new unallocated root cause: **none proven**;
- PD7 current-channel variant: **REVALIDATION-REQUIRED / rollout-sensitive**;
- C42: **canonical NOT-TRIAGED / UNKNOWN; exact external terminalization/evidence still required**;
- project-wide unconditional coverage-complete state: **not currently justified**;
- release readiness: **NOT READY**.

The next research action is therefore not “continue adding random families”. It is to close the two named reconciliation blockers with the exact evidence available for each boundary.
