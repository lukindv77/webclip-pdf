# P1-231 S0-B PSL cross-platform portability re-proof — 2026-09-21

Status: **PHYSICAL PORTABILITY PASS / AUTHORITY RECONCILED; GENERATOR-RCF BLOCKER ACTIVE**

## Scope

This evidence closes the physical byte-portability question for the corrected Public Suffix List generator only.

It does not activate release policy, build a product archive, advance Chrome/Yandex QA, or close P1-231.

## Exact evidence generation

Temporary evidence PR: #309

Exact evidence head:

`5cdbb131513f77cacac91c6b229ad7dd77b55420`

Temporary workflow run:

`P1-231 PSL portability evidence #1`

GitHub Actions run id:

`35566109810`

The temporary workflow was permitted only as environment-bound evidence collection and is removed before the final mergeable PR head, per repository policy.

## Exact generation roots

Both platform jobs resolved the same exact Git objects:

- generator: `project_tools/build_public_suffix_js.py`
  - Git blob OID: `79b279fdc90dce39acb671d294dd814357dce380`
  - SHA-256: `efc6da5fb5b48f4ba85517b0596aaebe3180e514f9ee945b0b33273b988a67ba`
- input: `public_suffix_list.dat`
  - Git blob OID: `7658ddd3081291afbb4090c1caa136707ff71d25`
  - SHA-256: `14ef61b1c212f701f3636c1d01ab9254daf841f57eb6433bcbbef56c726ca656`
- committed output: `public-suffix.js`
  - Git blob OID: `541a0833e4731e3513d327208904661cc3d3e990`
  - SHA-256: `72aea4d8a8505ad90d9070bca539dff7d49391f034d0dd41f76d64867efc0b26`
  - bytes: `167388`
  - committed CRLF count: `0`

The witness materialized only the exact generator/input Git blobs in an isolated temporary workspace and compared raw generated bytes directly with the exact committed output blob. No newline or text normalization was performed.

## Linux observation

Job id:

`106228144739`

Runner:

`ubuntu-24.04`

Observed platform:

`Linux 6.17.0-1022-azure`

Runtime:

`CPython 3.12.10`

OS line separator hex:

`0a`

Result:

- generated SHA-256 = `72aea4d8a8505ad90d9070bca539dff7d49391f034d0dd41f76d64867efc0b26`
- committed output SHA-256 = same
- exact raw byte equality = true
- generated/committed CRLF count = 0

## Windows observation

Job id:

`106228144631`

Runner:

`windows-2025`

Observed platform:

`Windows 2025Server`

Runtime:

`CPython 3.12.10`

OS line separator hex:

`0d0a`

Result:

- generated SHA-256 = `72aea4d8a8505ad90d9070bca539dff7d49391f034d0dd41f76d64867efc0b26`
- committed output SHA-256 = same
- exact raw byte equality = true
- generated/committed CRLF count = 0

This is the decisive negative control against the historical defect: the Windows process had CRLF as the platform line separator, yet the corrected byte writer emitted the exact LF-only committed blob.

## Comparison with historical failed portability state

The historical generator used text-mode `Path.write_text(...)`, and the earlier Windows probe produced CRLF-translated bytes.

The corrected generator uses:

`OUT.write_bytes(code.encode('utf-8'))`

The new matrix proves the corrected exact generator/input/output generation roots are byte-portable across the required Linux/Windows pair under the pinned CPython 3.12.10 profile.

Therefore the physical proposition:

`current_psl_windows_portable=false`

is no longer current for these exact generation roots.

## Important authority boundary

This evidence does **not** by itself make P1-231 release-authoritative.

The existing generation-portability binding refinement separately requires executable generator authority to be covered by full RCF before production candidate admission can be treated as complete. The current full-RCF bootstrap still does not bind `project_tools/build_public_suffix_js.py`.

Accordingly, this tranche records the physical portability PASS but does not silently reuse or fabricate blocker-review/release-decision authority.

A dedicated authority-reconciliation tranche must update S0-B/S0-F and downstream current-state models so the portability blocker is retired and the next fail-closed blocker is represented explicitly.

## Identity and release impact

The evidence workflow and witness are outside the extension package. Generator/input/output package bytes used by the evidence are unchanged from the corrected baseline.

Current package identity remains:

- package files: 34
- RPF: `sha256:feab25126c9d686062f8ed0da8c9d7ad39f468ebc819342bb34fbd2c47e0e843`

The historical 33-file RPF remains legacy/incomplete control only:

`sha256:b65c38854c016ce3ea88efd1caf5c3291a3089336ba9d58b01b9f86db73b835a`

This physical proof does not advance Chrome QCF, Yandex QCF, current full RCF or BCF.

P1-231 remains **ACTIVE**. Manifest remains `0.9.8`. Release readiness remains **NOT READY**.

No product ZIP/build, version bump, tag, deployment, GitHub Release, release-gate execution, Chrome qualification, Yandex mutation/qualification or release decision was performed.


## Authority reconciliation on current P1-231 models

A follow-on bounded tranche consumes this exact physical proof without changing the generator/input/output roots.

Current authority interpretation is now:

- S0-B portability marker: `current_psl_windows_portable=true`;
- S0-F exact source-generation portability prerequisite: satisfied for the current exact roots;
- current S0-F gate: `blocked-generator-rcf`;
- current fail-closed reason: `SOURCE_GENERATION_GENERATOR_NOT_RCF_BOUND`;
- S0-H remains blocked before package load/build;
- S1 shadow stages remain candidate-ineligible.

This does not turn the portability observation into release authority. The executable generator `project_tools/build_public_suffix_js.py` is still outside the current full-RCF input set, so the candidate cannot be admitted until the release-contract authority is reconciled and affected RCF-dependent evidence is settled under the existing rules.

The historical `SOURCE_GENERATION_PORTABILITY_UNPROVEN` branch remains a negative fail-closed contract for future candidates without required portability proof. It is no longer the current blocker for the exact roots above.

P1-231 remains **ACTIVE**; manifest remains `0.9.8`; release readiness remains **NOT READY**. No build/ZIP, version bump, release gate, tag, deployment, GitHub Release, Chrome qualification, Yandex mutation/qualification or release decision is authorized by this reconciliation.
