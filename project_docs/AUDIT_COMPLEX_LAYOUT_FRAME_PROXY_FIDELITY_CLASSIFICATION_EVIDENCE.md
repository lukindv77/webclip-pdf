# Durable classification evidence — complex layout / flattened iframe fidelity

This file is machine-recognized durable audit evidence. `AUDIT_REGISTRY.md` remains the sole authority for current P-code ownership and status. Detailed 38-block proof is preserved in `AUDIT_COMPLEX_LAYOUT_FRAME_PROXY_FIDELITY_EVIDENCE_2026-08-29.md`.

Audited source baseline: `main` at `ca906a363e88f5ce78281633370dbb644c080cae`.

## Classification

No new P-number and no status transition are justified by this tranche.

- **P0-004** owns selected-copy fidelity manifestations where removing unselected context recomputes inline/flex/grid/table geometry, destroys blend/backdrop appearance, or causes fixed screen content to repeat under paged PDF semantics.
- **P1-187** owns flattened same-origin iframe rendered-state loss: flex/grid/gaps, writing direction, visual effects, pseudo/marker content, current controls/canvas, object geometry, frame-local URL/resource provenance, fixed/static consistency and truthful handling of the 2500-element style-materialization budget.
- **P1-160** owns the auto-content discovery refinement where a meaningful `display:contents` semantic root has visible descendants but a 0x0 own rectangle and can be rejected while a broad BODY fallback wins.
- **P1-003** owns additional visual-resource readiness cases such as delayed `border-image`, pseudo `content:url(...)` and external `clip-path:url(...)`; transport-inconclusive SVG probes were not promoted.
- **P2-007** remains the architecture/product owner for explicit current-view versus complete-static/Reader/archive capture-mode semantics.

P1-228 is not allocated by this tranche. This file does not imply that any absent P-number is globally free.

## Validation boundary

Managed Chromium semantic probes and external capture-engine/user reports support the evidence but do not replace real unpacked Chrome, optional-host-permission/cross-origin QA, GPU-dependent cases or real Yandex E2E. Runtime, manifest, version, build/tag/release state and release readiness are unchanged.
